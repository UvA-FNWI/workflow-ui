apiVersion: v1
kind: ConfigMap
metadata:
  name: workflow-ui-configmap
data:
  env-contents: |
    window._env = {
        VITE_WEBAPI_URL: "{{ .Values.envVars.webapiUrl }}",
        VITE_ENV: "{{ .Values.envVars.env }}"
    };
